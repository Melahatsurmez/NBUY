<!DOCTYPE html>
<html lang="en">
@* Burada _header.cshtml partial viewımızı çağırıyoruz. *@
@* <partial name="_Header"/> *@
@await Html.PartialAsync("_Header") 
@* Yukardaki çalışırken o satır çalıştıktan sonra sıradaki satıra geçer ama partialla yapılan çalışması bitmesede alttaki satıra geçer *@
<body>
    @await RenderSectionAsync("FirstSection",false)
    <partial name="_HeaderNavBar"/>
    <div class="container">
        <main role="main" class="pb-3">
            @RenderBody()
        </main>
    </div>
    <partial name="_Footer"/>
    @await RenderSectionAsync("Scripts", required: false)

</body>

</html>
